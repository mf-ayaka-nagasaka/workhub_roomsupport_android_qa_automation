# language: ja
機能: 予約認証モードでの初期設定
  管理者がタブレットの初回起動時に、認証方式の選択・設定タイプの選択・
  各種設定の入力を行い、予約認証モードで初期設定を完了する。

  背景:
    前提 タブレットは初期設定が未完了の状態である

  # ────────────────────────────────────────
  # ルール①: 管理者はQRまたは顔認証方式を選択して初期設定を開始できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：QR認証を選択して初期設定を開始
  シナリオ: QR認証方式を選択すると設定タイプ選択に進める
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中がQR認証を選択する
    ならば 設定タイプ選択ステップが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：顔認証を選択して有効化フローへ
  シナリオ: 顔認証方式を選択すると顔認証有効化フローに進む
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中が顔認証を選択する
    ならば 顔認証の有効化フローが開始される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：QR＋顔認証を選択して有効化フローへ
  シナリオ: QR＋顔認証方式を選択すると顔認証有効化フローに進む
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中がQR＋顔認証を選択する
    ならば 顔認証の有効化フローが開始される

    # 📐 観点：条件分岐遷移
    前提 管理者が認証方式選択ステップにいる
    もし QR認証を選択する
    ならば 設定タイプ選択ステップが表示される（顔認証有効化をスキップ）

    前提 管理者が認証方式選択ステップにいる
    もし 顔認証を選択する
    ならば 顔認証の有効化フローが開始される

    前提 管理者が認証方式選択ステップにいる
    もし QR＋顔認証を選択する
    ならば 顔認証の有効化フローが開始される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：認証方式選択中に中断→再開
  シナリオ: 認証方式選択中にアプリを中断して再開すると認証方式選択から再開される
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中がアプリを中断して再開する
    ならば 認証方式選択ステップが表示される

    # 📐 観点：中断→再開時の状態復元
    前提 管理者が認証方式選択ステップにいる
    もし アプリを中断して再開する
    ならば 認証方式選択画面から再開される

  # ────────────────────────────────────────
  # ルール②: 顔認証を選択した管理者は、有効化を完了しなければ設定タイプ選択に進めない
  # ────────────────────────────────────────

  # @TestLevel: ApplicationUnit (JVM)
  # 🟢 正常系：顔認証有効化を完了
  シナリオ: 顔認証の有効化を完了すると設定タイプ選択に進める
    前提 管理者 "田中" が顔認証有効化ステップにいる
    もし 田中が顔認証の有効化を確定する
    ならば 有効化完了が表示される
    かつ 設定タイプ選択ステップに進める

    # 📐 観点：前提条件の充足
    前提 管理者が顔認証有効化ステップにいる
    もし 有効化を完了する
    ならば 設定タイプ選択ステップに進める

    前提 管理者が顔認証有効化ステップにいる
    もし 有効化処理中にアプリを中断する
    ならば 有効化は完了せず、再度有効化ステップが表示される

    前提 管理者が顔認証有効化ステップにいる
    もし 有効化をスキップしようとする
    ならば スキップできず、有効化ステップに留まること

  # @TestLevel: ApplicationUnit (JVM)
  # 🟢 正常系：顔認証有効化をキャンセル
  シナリオ: 顔認証の有効化をキャンセルすると認証方式選択に戻る
    前提 管理者 "田中" が顔認証有効化ステップにいる
    もし 田中が顔認証の有効化をキャンセルする
    ならば 認証方式選択ステップに戻る

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：顔認証説明画面で中断→再開
  シナリオ: 顔認証の説明画面でアプリを中断して再開すると認証方式選択に戻る
    前提 管理者 "田中" が顔認証の説明画面にいる
    もし 田中がアプリを中断して再開する
    ならば 認証方式選択ステップが表示される

    # 📐 観点：フェーズ境界での保持粒度
    前提 管理者が顔認証の説明画面にいる
    もし アプリを中断して再開する
    ならば 説明画面ではなく認証方式選択画面に戻る

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：顔認証有効化リクエスト送信後に中断→再開
  シナリオ: 顔認証有効化リクエスト送信後にアプリを中断して再開すると有効化画面から再開される
    前提 管理者 "田中" が顔認証有効化リクエストを送信済みである
    もし 田中がアプリを中断して再開する
    ならば 顔認証有効化ステップが表示される

    # 📐 観点：中断→再開時の状態復元
    前提 管理者が顔認証有効化リクエストを送信済みである
    もし アプリを中断して再開する
    ならば 顔認証有効化画面から再開される

  # ────────────────────────────────────────
  # ルール③: 管理者は設定タイプ（テンプレート／コピー／カスタム）を選んで設定を構成できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：テンプレートを選択して設定を構成
  シナリオ: テンプレートを選択するとその設定値が適用された設定画面に遷移する
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    もし 田中がテンプレートを選択する
    ならば テンプレートの設定値が適用された設定画面が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：既存設定をコピーして設定を構成
  シナリオ: 既存設定のコピーを選択するとコピー元の設定値が適用された設定画面に遷移する
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    かつ 他のタブレットで保存された既存設定が1件以上存在する
    もし 田中がコピーを選択し、コピー元の設定を指定する
    ならば コピー元の設定値が適用された設定画面が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：カスタムを選択して設定を構成
  シナリオ: カスタムを選択すると手動設定の設定画面に遷移する
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    もし 田中がカスタムを選択する
    ならば 各項目を手動で入力する設定画面が表示される

    # 📐 観点：選択操作と確定
    前提 設定タイプ選択ステップでテンプレートを選択した状態である
    もし コピーに選択を変更する
    ならば コピー元の設定値が適用された設定画面が表示される

    前提 候補に長い設定名のコピー元が含まれている
    もし 長い設定名のコピー元を選択する
    ならば コピー元の設定値が適用された設定画面が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：既存設定がない場合コピー選択肢が非表示
  シナリオ: 既存設定が存在しない場合はコピー選択肢が表示されない
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    かつ 他のタブレットで保存された既存設定が存在しない
    ならば コピー選択肢は表示されない

    # 📐 観点：データ存在依存の表示制御
    前提 保存された既存設定が存在しない場合
    ならば コピー選択肢は表示されない

    前提 保存された既存設定が1件存在する場合
    ならば コピー選択肢が表示され、1件の設定が候補として表示される

    前提 保存された既存設定が多数存在する場合
    ならば コピー選択肢が表示され、全件の設定が候補として表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：設定タイプ選択中に中断→再開
  シナリオ: 設定タイプ選択中にアプリを中断して再開すると設定タイプ選択から再開される
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    もし 田中がアプリを中断して再開する
    ならば 設定タイプ選択ステップが表示される

    # 📐 観点：中断→再開時の状態復元
    前提 管理者が設定タイプ選択ステップにいる
    もし アプリを中断して再開する
    ならば 設定タイプ選択画面から再開される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：ひな型選択中に中断→再開
  シナリオ: ひな型選択画面でアプリを中断して再開すると設定タイプ選択に戻る
    前提 管理者 "田中" がひな型選択画面にいる
    もし 田中がアプリを中断して再開する
    ならば 設定タイプ選択ステップが表示される

    # 📐 観点：フェーズ境界での保持粒度
    前提 管理者がひな型選択画面にいる
    もし アプリを中断して再開する
    ならば ひな型選択画面ではなく設定タイプ選択画面に戻る

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：コピー元選択中に中断→再開
  シナリオ: コピー元選択画面でアプリを中断して再開すると設定タイプ選択に戻る
    前提 管理者 "田中" がコピー元選択画面にいる
    もし 田中がアプリを中断して再開する
    ならば 設定タイプ選択ステップが表示される

    # 📐 観点：フェーズ境界での保持粒度
    前提 管理者がコピー元選択画面にいる
    もし アプリを中断して再開する
    ならば コピー元選択画面ではなく設定タイプ選択画面に戻る

  # ────────────────────────────────────────
  # ルール④: 予約認証モードで保存するには、選択スペースに予約設定が登録されていなければならない
  # ────────────────────────────────────────

  # @TestLevel: DomainUnit (JVM)
  # 🟢 正常系：予約設定のあるスペースで保存成功
  シナリオ: 予約設定が登録されたスペースを選択して予約認証モードで保存できる
    前提 管理者 "田中" が設定画面で予約認証モードの必須項目をすべて入力している
    かつ 選択されたスペース "会議室A" には予約設定が登録されている
    もし 田中が設定の保存を実行する
    ならば 設定が保存される
    かつ 設定コピー確認が表示される

  # @TestLevel: DomainUnit (JVM)
  # 🔴 異常系：予約設定のないスペースで保存拒否
  シナリオ: 予約設定が未登録のスペースでは予約認証モードの保存が拒否される
    前提 管理者 "田中" が設定画面で予約認証モードを選択している
    かつ 選択されたスペース "会議室B" には予約設定が登録されていない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "会議室Bは予約設定がされていません" が表示される
    かつ 設定は保存されない

    # 📐 観点：ビジネスルールバリデーション
    前提 予約設定が登録されているスペースを選択した場合
    もし 設定の保存を実行する
    ならば 設定が保存される

    前提 予約設定が未登録のスペースを選択した場合
    もし 設定の保存を実行する
    ならば エラーメッセージ "{スペース名}は予約設定がされていません" が表示される

    前提 複数スペースのうち一部に予約設定がない場合
    もし 予約設定のないスペースを選択して保存する
    ならば エラーメッセージが表示され保存されない

    # 📐 観点：エラーメッセージの適切性
    ならば エラーメッセージ "会議室Bは予約設定がされていません" が他のエラーと区別可能な一意の文言であること
    ならば エラーメッセージが画面の適切な位置に表示されること
    ならば エラーメッセージが適切な時間表示される（または手動で閉じるまで表示される）こと
    ならば 多言語設定時にエラーメッセージが設定言語で表示されること

  # ────────────────────────────────────────
  # ルール⑤: 初期設定の保存には必須項目の入力と予約認証方法の選択が必要
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：予約認証方法の未選択で保存拒否
  シナリオ: 予約認証方法を選択せずに保存するとバリデーションエラーになる
    前提 管理者 "田中" が設定画面で予約認証方法を選択していない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "予約認証の認証方法を選択してください" が表示される
    かつ 設定は保存されない

    # 📐 観点：必須選択バリデーション
    前提 予約認証方法が未選択の状態である
    もし 設定の保存を実行する
    ならば エラーメッセージ "予約認証の認証方法を選択してください" が表示される

    前提 予約認証方法を1件選択した後に選択を解除した場合
    もし 設定の保存を実行する
    ならば エラーメッセージ "予約認証の認証方法を選択してください" が表示される

    # 📐 観点：エラーメッセージの適切性
    ならば エラーメッセージ "予約認証の認証方法を選択してください" が他のエラーと区別可能な一意の文言であること
    ならば エラーメッセージが画面の適切な位置に表示されること
    ならば エラーメッセージが適切な時間表示される（または手動で閉じるまで表示される）こと
    ならば 多言語設定時にエラーメッセージが設定言語で表示されること

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：タブレット名の未入力で保存拒否
  シナリオ: タブレット名を入力せずに保存するとバリデーションエラーになる
    前提 管理者 "田中" が設定画面でタブレット名を入力していない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "タブレット名を入力してください" が表示される
    かつ 設定は保存されない

    # 📐 観点：未入力バリデーション
    前提 タブレット名が空の状態である
    もし 設定の保存を実行する
    ならば エラーメッセージ "タブレット名を入力してください" が表示される

    前提 タブレット名に空白文字のみが入力されている
    もし 設定の保存を実行する
    ならば エラーメッセージ "タブレット名を入力してください" が表示される

    前提 タブレット名を入力後に全て消去した
    もし 設定の保存を実行する
    ならば エラーメッセージ "タブレット名を入力してください" が表示される

    # 📐 観点：エラーメッセージの適切性
    ならば エラーメッセージ "タブレット名を入力してください" が他のエラーと区別可能な一意の文言であること
    ならば エラーメッセージが画面の適切な位置に表示されること
    ならば エラーメッセージが適切な時間表示される（または手動で閉じるまで表示される）こと
    ならば 多言語設定時にエラーメッセージが設定言語で表示されること

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：スペース未選択で保存拒否
  シナリオ: スペースを選択せずに保存するとバリデーションエラーになる
    前提 管理者 "田中" が設定画面でスペースを選択していない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "空間を選択してください" が表示される
    かつ 設定は保存されない

    # 📐 観点：必須選択バリデーション
    前提 スペースが未選択の状態である
    もし 設定の保存を実行する
    ならば エラーメッセージ "空間を選択してください" が表示される

    # 📐 観点：エラーメッセージの適切性
    ならば エラーメッセージ "空間を選択してください" が他のエラーと区別可能な一意の文言であること
    ならば エラーメッセージが画面の適切な位置に表示されること
    ならば エラーメッセージが適切な時間表示される（または手動で閉じるまで表示される）こと
    ならば 多言語設定時にエラーメッセージが設定言語で表示されること

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：パスワード不一致で保存拒否
  シナリオ: パスワードと確認パスワードが一致しない場合は保存が拒否される
    前提 管理者 "田中" が設定画面でパスワードに "pass123" を入力している
    かつ 確認パスワードに "pass456" を入力している
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "パスワードが一致しません" が表示される
    かつ 設定は保存されない

    # 📐 観点：入力値一致検証
    前提 パスワードと確認パスワードが一致している場合
    もし 設定の保存を実行する
    ならば パスワード一致のバリデーションエラーは発生しない

    前提 パスワードと確認パスワードが不一致の場合
    もし 設定の保存を実行する
    ならば エラーメッセージ "パスワードが一致しません" が表示される

    前提 パスワードのみ入力し確認パスワードが空の場合
    もし 設定の保存を実行する
    ならば エラーメッセージ "パスワードが一致しません" が表示される

    前提 両方空の場合
    もし 設定の保存を実行する
    ならば パスワード一致のバリデーションエラーは発生しない

    前提 コピー＆ペーストでパスワードを確認欄に貼り付けた場合
    もし 設定の保存を実行する
    ならば パスワード一致のバリデーションエラーは発生しない

    # 📐 観点：エラーメッセージの適切性
    ならば エラーメッセージ "パスワードが一致しません" が他のエラーと区別可能な一意の文言であること
    ならば エラーメッセージが画面の適切な位置に表示されること
    ならば エラーメッセージが適切な時間表示される（または手動で閉じるまで表示される）こと
    ならば 多言語設定時にエラーメッセージが設定言語で表示されること

  # ────────────────────────────────────────
  # ルール⑥: 初期設定の保存成功後、設定コピーの登録有無を選択して初期設定を完了できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：設定コピーを登録して初期設定完了
  シナリオ: 保存後に設定コピーを登録して初期設定を完了できる
    前提 管理者 "田中" が設定の保存に成功し、設定コピー確認が表示されている
    もし 田中が「コピーする」を選択し、識別名称 "本社1F会議室設定" を入力して登録する
    ならば 設定コピーが登録される
    かつ 初期設定完了が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：設定コピーをせずに初期設定完了
  シナリオ: 保存後に設定コピーをしないで初期設定を完了できる
    前提 管理者 "田中" が設定の保存に成功し、設定コピー確認が表示されている
    もし 田中が「コピーしない」を選択する
    ならば 初期設定完了が表示される

    # 📐 観点：保存後の付帯操作
    前提 設定の保存に成功し、設定コピー確認が表示されている
    もし 「コピーする」を選択し、識別名称を入力して登録する
    ならば 設定コピーが登録され、初期設定完了が表示される

    前提 設定の保存に成功し、設定コピー確認が表示されている
    もし 「コピーしない」を選択する
    ならば コピーせずに初期設定完了が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：識別名称未入力でコピー登録不可
  シナリオ: 識別名称を入力しなければ設定コピーの登録ができない
    前提 管理者 "田中" が設定の保存に成功し、「コピーする」を選択している
    かつ 識別名称が未入力である
    ならば 登録ボタンは無効化されている

    # 📐 観点：未入力バリデーション
    前提 識別名称が空の状態である
    ならば 登録ボタンは無効化されている

    前提 識別名称に空白文字のみが入力されている
    ならば 登録ボタンは無効化されている

    前提 識別名称を入力後に全て消去した
    ならば 登録ボタンは無効化されている

  # ────────────────────────────────────────
  # ルール⑦: ON/OFF切替で子項目の表示が連動する設定（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動ステータス更新を有効にすると子項目群が表示される
  シナリオ: 初期設定で自動ステータス更新を有効にすると自動キャンセル・自動延長の設定が表示される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中が自動ステータス更新を有効にする
    ならば 自動キャンセルと自動延長の設定項目群が表示される

    # 📐 観点：親子連動の表示制御
    # → 2階層ネスト: 親(自動ステータス更新) → 子(自動キャンセル, 自動延長)
    前提 自動ステータス更新がOFFの状態である
    もし 自動ステータス更新をONにする
    ならば 自動キャンセルON/OFFと自動延長ON/OFFが表示される

    # → 3階層ネスト: 自動キャンセル → 前通知 → 時間
    前提 自動ステータス更新がON、自動キャンセルがOFFの状態である
    もし 自動キャンセルをONにし、さらに前通知をONにする
    ならば 自動キャンセル時間と前通知ON/OFFが表示され、前通知時間の入力欄も表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動ステータス更新を無効にすると子項目群が非表示になる
  シナリオ: 初期設定で自動ステータス更新を無効にすると自動キャンセル・自動延長と全子項目が非表示になる
    前提 管理者 "田中" が初期設定の設定画面で自動ステータス更新を有効にしている
    かつ 自動キャンセルと自動延長の設定項目が表示されている
    もし 田中が自動ステータス更新を無効にする
    ならば 自動キャンセル・自動延長と全子項目が非表示になる
    かつ 非表示になった項目はデフォルト値に復元される

    # 📐 観点：親子連動の表示制御
    # → 親OFF時のデフォルト復元
    前提 自動ステータス更新がONで、自動キャンセル=ON・時間=300秒に設定済みである
    もし 自動ステータス更新をOFFにする
    ならば 子項目が非表示になる
    かつ 再度ONにすると自動キャンセルはOFF（デフォルト値）に戻っている

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動キャンセルを有効にすると時間と前通知が表示される
  シナリオ: 初期設定で自動キャンセルを有効にすると自動キャンセル時間と前通知設定が表示される
    前提 管理者 "田中" が初期設定の設定画面で自動ステータス更新が有効の状態である
    もし 田中が自動キャンセルを有効にする
    ならば 自動キャンセル時間（秒）と自動キャンセル前通知のON/OFFが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動キャンセル前通知を有効にすると通知時間が表示される
  シナリオ: 初期設定で自動キャンセル前通知を有効にすると前通知時間の入力欄が表示される
    前提 管理者 "田中" が初期設定の設定画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル前通知を有効にする
    ならば 前通知時間（秒）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動延長を有効にすると時間設定が表示される
  シナリオ: 初期設定で自動延長を有効にすると延長タイミングと延長時間の入力欄が表示される
    前提 管理者 "田中" が初期設定の設定画面で自動ステータス更新が有効の状態である
    もし 田中が自動延長を有効にする
    ならば 自動延長タイミング（分）と延長時間（分）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：終了前リマインダーを有効にすると通知設定が表示される
  シナリオ: 初期設定で終了前リマインダーを有効にすると通知時間・繰り返し・音の設定が表示される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が終了前リマインダーを有効にする
    ならば 通知時間（秒）と繰り返し設定とリマインダー音の選択が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：リマインダー繰り返しを回数指定にすると回数入力が表示される
  シナリオ: 初期設定でリマインダー繰り返しを回数指定にすると回数入力欄が表示される
    前提 管理者 "田中" が初期設定の設定画面で終了前リマインダーを有効にしている
    もし 田中がリマインダーの繰り返しを「回数を指定」に切替える
    ならば リマインダー回数（回）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠機能をONにするとデバイスPickerが表示される
  シナリオ: 初期設定で解錠機能を解錠するに切替えると解錠デバイスPickerが表示される
    前提 管理者 "田中" が初期設定の設定画面でデバイス・解施錠セクションを表示している
    もし 田中が解錠機能を「解錠する」に切替える
    ならば 解錠対象デバイス1のPickerが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠機能をOFFにするとデバイスPickerが非表示になる
  シナリオ: 初期設定で解錠機能を解錠しないに切替えると解錠デバイスPickerが非表示になる
    前提 管理者 "田中" が初期設定の設定画面で解錠機能を「解錠する」にしている
    もし 田中が解錠機能を「解錠しない」に切替える
    ならば 解錠対象デバイスPickerが非表示になる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：常時接続を有効にすると対象デバイスが表示される
  シナリオ: 初期設定で常時接続を有効にすると常時接続対象デバイスの選択が表示される
    前提 管理者 "田中" が初期設定の設定画面でデバイス・解施錠セクションを表示している
    もし 田中が常時接続を有効にする
    ならば 常時接続対象デバイスの選択Pickerが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：なりすまし防止を有効にすると詳細設定が表示される
  シナリオ: 初期設定でなりすまし防止を有効にすると防止モード等の設定が表示される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中がなりすまし防止を有効にする
    ならば 防止モード（ハイセキュリティ/スムーズ）と防止期間の設定が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：省エネモードをカスタムにすると条件Pickerが表示される
  シナリオ: 初期設定で省エネモードをカスタムにすると開始・停止条件のPickerが表示される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中が省エネモードを「カスタム」に切替える
    ならば 開始バッテリー残量・開始温度状態・停止バッテリー残量・停止温度状態のPickerが表示される

    # 📐 観点：親子連動の表示制御
    # → 親設定依存のPicker表示（省エネモード → カスタム条件Picker）
    前提 省エネモードが「カスタム」でPicker群が表示されている
    もし 省エネモードを「オフ」に戻す
    ならば 4つの条件Pickerが非表示になる

  # ────────────────────────────────────────
  # ルール⑧: 必須数値フィールドのバリデーション（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：数値フィールドの値を入力して保存
  シナリオ: 初期設定で数値フィールドの値を入力して保存すると設定が反映される
    前提 管理者 "田中" が初期設定の設定画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル時間を有効な値に入力して保存を実行する
    ならば 入力した自動キャンセル時間が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：1以上が必要なフィールドに0以下を入力
  シナリオ: 初期設定で自動キャンセル時間に0以下を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が初期設定の設定画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル時間に "0" を入力して保存を実行する
    ならば エラー「自動キャンセル時間は1以上の値を設定してください」が表示される

    # 📐 観点：数値バリデーション
    # → 境界値: 0（NG）と 1（OK）
    前提 自動キャンセル時間の入力欄が表示されている
    もし "0" を入力して保存する
    ならば エラー「自動キャンセル時間は1以上の値を設定してください」が表示される

    前提 自動キャンセル時間の入力欄が表示されている
    もし "1" を入力して保存する
    ならば エラーは表示されず保存が成功する

    # → 負数
    前提 自動キャンセル時間の入力欄が表示されている
    もし "-5" を入力して保存する
    ならば エラー「自動キャンセル時間は1以上の値を設定してください」が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証必要時間に負の値を入力
  シナリオ: 初期設定で認証必要時間に負の値を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が認証必要時間に "-1" を入力して保存を実行する
    ならば エラー「認証必要時間は0以上の値を設定してください」が表示される

    # 📐 観点：数値バリデーション
    # → 0以上ルール: 0（OK）と -1（NG）
    前提 認証必要時間の入力欄が表示されている
    もし "0" を入力して保存する
    ならば エラーは表示されず保存が成功する

    前提 認証必要時間の入力欄が表示されている
    もし "-1" を入力して保存する
    ならば エラー「認証必要時間は0以上の値を設定してください」が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証成功類似度に範囲外の値を入力
  シナリオ: 初期設定で認証成功類似度に0〜1の範囲外の値を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が認証成功類似度に "1.5" を入力して保存を実行する
    ならば エラー「認証成功類似度は0〜1の間の値を設定してください」が表示される

    # 📐 観点：数値バリデーション
    # → 範囲指定(0〜1): 境界値テスト
    前提 認証成功類似度の入力欄が表示されている
    もし "0" を入力して保存する
    ならば エラーは表示されず保存が成功する

    前提 認証成功類似度の入力欄が表示されている
    もし "1" を入力して保存する
    ならば エラーは表示されず保存が成功する

    前提 認証成功類似度の入力欄が表示されている
    もし "1.1" を入力して保存する
    ならば エラー「認証成功類似度は0〜1の間の値を設定してください」が表示される

    前提 認証成功類似度の入力欄が表示されている
    もし "-0.1" を入力して保存する
    ならば エラー「認証成功類似度は0〜1の間の値を設定してください」が表示される

  # ────────────────────────────────────────
  # ルール⑨: 単独の切替・選択設定の変更と保存（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：テナント従業員による予約の設定
  シナリオ: 初期設定でテナント従業員による予約を不許可に設定して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で認証セクションを表示している
    もし 田中がテナント従業員による予約を「不許可」に設定して保存を実行する
    ならば テナント従業員による予約の不許可設定が保存される

    # 📐 観点：単独設定の切替と保存
    # → 許可→不許可の切替
    前提 テナント従業員による予約が「許可」の状態である
    もし 「不許可」に切替えて保存する
    ならば 不許可設定が保存される

    # → 不許可→許可の切替（逆方向）
    前提 テナント従業員による予約が「不許可」の状態である
    もし 「許可」に切替えて保存する
    ならば 許可設定が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約情報の表示設定
  シナリオ: 初期設定で予約情報の表示を非表示に設定して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中が予約情報の表示を「非表示」に設定して保存を実行する
    ならば 予約情報の非表示設定が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：今すぐ入室の解錠タイミング設定（3択）
  シナリオ: 初期設定で今すぐ入室の解錠タイミングを設定して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で予約作成（今すぐ入室）が有効かつ解錠機能が有効の状態である
    もし 田中が今すぐ入室の解錠タイミングを「ブッキング防止」に設定して保存を実行する
    ならば 解錠タイミングのブッキング防止設定が保存される

    # 📐 観点：単独設定の切替と保存
    # → 3択以上切替: 即時/ブッキング防止/実績管理
    前提 解錠タイミングが「即時」の状態である
    もし 「ブッキング防止」に切替えて保存する
    ならば ブッキング防止設定が保存される

    前提 解錠タイミングが「ブッキング防止」の状態である
    もし 「実績管理」に切替えて保存する
    ならば 実績管理設定が保存される

  # ────────────────────────────────────────
  # ルール⑩: CheckBox群の個別ON/OFFと機能制御（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：タブレットからの機能制御の個別切替
  シナリオ: 初期設定でタブレットからの機能制御を個別にON/OFFして保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中がタブレットからの機能制御（入室/退室/予約作成/予約延長/予約削除）を個別にON/OFF切替して保存を実行する
    ならば 各機能の有効/無効が保存される

    # 📐 観点：CheckBox群の個別制御
    # → 個別ON-OFF: 特定の機能のみONにして残りはOFF
    前提 全機能がOFFの状態である
    もし 入室と退室のみONにして保存する
    ならば 入室=ON、退室=ON、他=OFFが保存される

    # → 全ON
    前提 全機能がOFFの状態である
    もし 全機能をONにして保存する
    ならば 全機能がONで保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約作成ONで解錠タイミングが表示される
  シナリオ: 初期設定で機能制御の予約作成をONにすると今すぐ入室の解錠タイミング選択が表示される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    かつ 予約作成（今すぐ入室）がOFFの状態である
    もし 田中が予約作成（今すぐ入室）をONにする
    ならば 今すぐ入室の解錠タイミング（即時/ブッキング防止/実績管理）の選択が表示される

    # 📐 観点：親子連動の表示制御
    # → CheckBoxのON/OFFで依存UIが連動
    前提 予約作成（今すぐ入室）がOFFの状態である
    もし ONにする
    ならば 解錠タイミング選択が表示される

    前提 予約作成（今すぐ入室）がONの状態である
    もし OFFにする
    ならば 解錠タイミング選択が非表示になる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：顔認証を必要とする機能の個別切替
  シナリオ: 初期設定で顔認証を必要とする機能を個別にON/OFFして保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が顔認証を必要とする機能（入室/退室/予約延長）を個別にON/OFF切替して保存を実行する
    ならば 各機能に顔認証が必要かどうかの設定が保存される

  # ────────────────────────────────────────
  # ルール⑪: Picker選択の設定と保存（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠対象デバイスの選択
  シナリオ: 初期設定で解錠対象デバイスを選択して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で解錠機能を「解錠する」にしている
    もし 田中が解錠対象デバイス1のPickerからデバイスを選択して保存を実行する
    ならば 選択したデバイスが保存される

    # 📐 観点：Picker選択と保存
    # → デバイス選択: 親設定依存のPicker
    前提 解錠機能が「解錠する」でPickerが表示されている
    もし デバイスAを選択して保存する
    ならば デバイスAが保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：リマインダー音の選択
  シナリオ: 初期設定でリマインダー音を選択して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面で終了前リマインダーを有効にしている
    もし 田中がリマインダー音のPickerから音を選択して保存を実行する
    ならば 選択した音が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：終了通知音の選択
  シナリオ: 初期設定で終了通知音を選択して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が終了通知音のPickerから音を選択して保存を実行する
    ならば 選択した音が保存される

  # ────────────────────────────────────────
  # ルール⑫: テキスト入力の文字数バリデーション（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：利用時間外メッセージを入力して保存
  シナリオ: 初期設定で利用時間外メッセージを100文字以内で入力して保存すると反映される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が利用時間外メッセージ（日本語）に100文字以内のテキストを入力して保存を実行する
    ならば メッセージが保存される

    # 📐 観点：文字数バリデーション
    # → 上限ちょうど: 100文字（OK）
    前提 利用時間外メッセージの入力欄が表示されている
    もし ちょうど100文字の日本語テキストを入力して保存する
    ならば エラーは表示されず保存が成功する

    # → 多言語混在: 日本語+英語混在で100文字以内
    前提 利用時間外メッセージの入力欄が表示されている
    もし 日本語と英語が混在した100文字以内のテキストを入力して保存する
    ならば エラーは表示されず保存が成功する

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：利用時間外メッセージが文字数超過
  シナリオ: 初期設定で利用時間外メッセージに101文字以上を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が利用時間外メッセージ（日本語）に101文字以上のテキストを入力して保存を実行する
    ならば エラー「利用時間外メッセージは100文字以内で入力してください」が表示される

    # 📐 観点：文字数バリデーション
    # → 上限超過: 101文字（NG）
    前提 利用時間外メッセージの入力欄が表示されている
    もし 101文字の日本語テキストを入力して保存する
    ならば エラー「利用時間外メッセージは100文字以内で入力してください」が表示される

    # → 英語のみ101文字: 多バイト・1バイト文字の文字数カウント確認
    前提 利用時間外メッセージの入力欄が表示されている
    もし 101文字の英語テキストを入力して保存する
    ならば エラー「利用時間外メッセージは100文字以内で入力してください」が表示される

  # ────────────────────────────────────────
  # E2E: ユーザーストーリー
  # ────────────────────────────────────────

  # @TestLevel: E2E (Cloud実機 / Nightly)
  # 🟢 正常系：ユーザーストーリー
  シナリオ: 【重要動線】初回起動から予約認証モードで初期設定を完了できる
    前提 タブレットは初期設定が未完了の状態である
    かつ 管理者 "田中" がすべての初期設定条件を満たしている
    もし 田中がQR認証を選択し、テンプレートで設定を構成し、予約設定のあるスペースを選択して保存する
    ならば 設定が保存され、初期設定完了が表示される
