# language: ja
機能: 予約認証モードでの初期設定
  管理者がタブレットの初回起動時に、認証方式の選択・設定タイプの選択・
  各種設定の入力を行い、予約認証モードで初期設定を完了する。

  背景:
    前提 タブレットは初期設定が未完了の状態である

  # ────────────────────────────────────────
  # ルール①: 管理者はQRまたは顔認証方式を選択して初期設定を開始できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：QR認証を選択して初期設定を開始
  シナリオ: QR認証方式を選択すると設定タイプ選択に進める
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中がQR認証を選択する
    ならば 設定タイプ選択ステップが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：顔認証を選択して有効化フローへ
  シナリオ: 顔認証方式を選択すると顔認証有効化フローに進む
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中が顔認証を選択する
    ならば 顔認証の有効化フローが開始される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：QR＋顔認証を選択して有効化フローへ
  シナリオ: QR＋顔認証方式を選択すると顔認証有効化フローに進む
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中がQR＋顔認証を選択する
    ならば 顔認証の有効化フローが開始される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：認証方式選択中に中断→再開
  シナリオ: 認証方式選択中にアプリを中断して再開すると認証方式選択から再開される
    前提 管理者 "田中" が初期設定の認証方式選択ステップにいる
    もし 田中がアプリを中断して再開する
    ならば 認証方式選択ステップが表示される

  # ────────────────────────────────────────
  # ルール②: 顔認証を選択した管理者は、有効化を完了しなければ設定タイプ選択に進めない
  # ────────────────────────────────────────

  # @TestLevel: ApplicationUnit (JVM)
  # 🟢 正常系：顔認証有効化を完了
  シナリオ: 顔認証の有効化を完了すると設定タイプ選択に進める
    前提 管理者 "田中" が顔認証有効化ステップにいる
    もし 田中が顔認証の有効化を確定する
    ならば 有効化完了が表示される
    かつ 設定タイプ選択ステップに進める

  # @TestLevel: ApplicationUnit (JVM)
  # 🟢 正常系：顔認証有効化をキャンセル
  シナリオ: 顔認証の有効化をキャンセルすると認証方式選択に戻る
    前提 管理者 "田中" が顔認証有効化ステップにいる
    もし 田中が顔認証の有効化をキャンセルする
    ならば 認証方式選択ステップに戻る

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：顔認証説明画面で中断→再開
  シナリオ: 顔認証の説明画面でアプリを中断して再開すると認証方式選択に戻る
    前提 管理者 "田中" が顔認証の説明画面にいる
    もし 田中がアプリを中断して再開する
    ならば 認証方式選択ステップが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：顔認証有効化リクエスト送信後に中断→再開
  シナリオ: 顔認証有効化リクエスト送信後にアプリを中断して再開すると有効化画面から再開される
    前提 管理者 "田中" が顔認証有効化リクエストを送信済みである
    もし 田中がアプリを中断して再開する
    ならば 顔認証有効化ステップが表示される

  # ────────────────────────────────────────
  # ルール③: 管理者は設定タイプ（テンプレート／コピー／カスタム）を選んで設定を構成できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：テンプレートを選択して設定を構成
  シナリオ: テンプレートを選択するとその設定値が適用された設定画面に遷移する
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    もし 田中がテンプレートを選択する
    ならば テンプレートの設定値が適用された設定画面が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：既存設定をコピーして設定を構成
  シナリオ: 既存設定のコピーを選択するとコピー元の設定値が適用された設定画面に遷移する
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    かつ 他のタブレットで保存された既存設定が1件以上存在する
    もし 田中がコピーを選択し、コピー元の設定を指定する
    ならば コピー元の設定値が適用された設定画面が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：カスタムを選択して設定を構成
  シナリオ: カスタムを選択すると手動設定の設定画面に遷移する
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    もし 田中がカスタムを選択する
    ならば 各項目を手動で入力する設定画面が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：既存設定がない場合コピー選択肢が非表示
  シナリオ: 既存設定が存在しない場合はコピー選択肢が表示されない
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    かつ 他のタブレットで保存された既存設定が存在しない
    ならば コピー選択肢は表示されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：設定タイプ選択中に中断→再開
  シナリオ: 設定タイプ選択中にアプリを中断して再開すると設定タイプ選択から再開される
    前提 管理者 "田中" が設定タイプ選択ステップにいる
    もし 田中がアプリを中断して再開する
    ならば 設定タイプ選択ステップが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：ひな型選択中に中断→再開
  シナリオ: ひな型選択画面でアプリを中断して再開すると設定タイプ選択に戻る
    前提 管理者 "田中" がひな型選択画面にいる
    もし 田中がアプリを中断して再開する
    ならば 設定タイプ選択ステップが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：コピー元選択中に中断→再開
  シナリオ: コピー元選択画面でアプリを中断して再開すると設定タイプ選択に戻る
    前提 管理者 "田中" がコピー元選択画面にいる
    もし 田中がアプリを中断して再開する
    ならば 設定タイプ選択ステップが表示される

  # ────────────────────────────────────────
  # ルール④: 予約認証モードで保存するには、選択スペースに予約設定が登録されていなければならない
  # ────────────────────────────────────────

  # @TestLevel: DomainUnit (JVM)
  # 🟢 正常系：予約設定のあるスペースで保存成功
  シナリオ: 予約設定が登録されたスペースを選択して予約認証モードで保存できる
    前提 管理者 "田中" が設定画面で予約認証モードの必須項目をすべて入力している
    かつ 選択されたスペース "会議室A" には予約設定が登録されている
    もし 田中が設定の保存を実行する
    ならば 設定が保存される
    かつ 設定コピー確認が表示される

  # @TestLevel: DomainUnit (JVM)
  # 🔴 異常系：予約設定のないスペースで保存拒否
  シナリオ: 予約設定が未登録のスペースでは予約認証モードの保存が拒否される
    前提 管理者 "田中" が設定画面で予約認証モードを選択している
    かつ 選択されたスペース "会議室B" には予約設定が登録されていない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "会議室Bは予約設定がされていません" が表示される
    かつ 設定は保存されない

  # ────────────────────────────────────────
  # ルール⑤: 初期設定の保存には必須項目の入力と予約認証方法の選択が必要
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：予約認証方法の未選択で保存拒否
  シナリオ: 予約認証方法を選択せずに保存するとバリデーションエラーになる
    前提 管理者 "田中" が設定画面で予約認証方法を選択していない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "予約認証の認証方法を選択してください" が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：タブレット名の未入力で保存拒否
  シナリオ: タブレット名を入力せずに保存するとバリデーションエラーになる
    前提 管理者 "田中" が設定画面でタブレット名を入力していない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "タブレット名を入力してください" が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：スペース未選択で保存拒否
  シナリオ: スペースを選択せずに保存するとバリデーションエラーになる
    前提 管理者 "田中" が設定画面でスペースを選択していない
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "空間を選択してください" が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：パスワード不一致で保存拒否
  シナリオ: パスワードと確認パスワードが一致しない場合は保存が拒否される
    前提 管理者 "田中" が設定画面でパスワードに "pass123" を入力している
    かつ 確認パスワードに "pass456" を入力している
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "パスワードが一致しません" が表示される
    かつ 設定は保存されない

  # ────────────────────────────────────────
  # ルール⑥: 初期設定の保存成功後、設定コピーの登録有無を選択して初期設定を完了できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：設定コピーを登録して初期設定完了
  シナリオ: 保存後に設定コピーを登録して初期設定を完了できる
    前提 管理者 "田中" が設定の保存に成功し、設定コピー確認が表示されている
    もし 田中が「コピーする」を選択し、識別名称 "本社1F会議室設定" を入力して登録する
    ならば 設定コピーが登録される
    かつ 初期設定完了が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：設定コピーをせずに初期設定完了
  シナリオ: 保存後に設定コピーをしないで初期設定を完了できる
    前提 管理者 "田中" が設定の保存に成功し、設定コピー確認が表示されている
    もし 田中が「コピーしない」を選択する
    ならば 初期設定完了が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：識別名称未入力でコピー登録不可
  シナリオ: 識別名称を入力しなければ設定コピーの登録ができない
    前提 管理者 "田中" が設定の保存に成功し、「コピーする」を選択している
    かつ 識別名称が未入力である
    ならば 登録ボタンは無効化されている

  # ────────────────────────────────────────
  # ルール⑦: ON/OFF切替で子項目の表示が連動する設定（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動ステータス更新を有効にすると子項目群が表示される
  シナリオ: 初期設定で自動ステータス更新を有効にすると自動キャンセル・自動延長の設定が表示される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中が自動ステータス更新を有効にする
    ならば 自動キャンセルと自動延長の設定項目群が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動ステータス更新を無効にすると子項目群が非表示になる
  シナリオ: 初期設定で自動ステータス更新を無効にすると自動キャンセル・自動延長と全子項目が非表示になる
    前提 管理者 "田中" が初期設定の設定画面で自動ステータス更新を有効にしている
    かつ 自動キャンセルと自動延長の設定項目が表示されている
    もし 田中が自動ステータス更新を無効にする
    ならば 自動キャンセル・自動延長と全子項目が非表示になる
    かつ 非表示になった項目はデフォルト値に復元される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動キャンセルを有効にすると時間と前通知が表示される
  シナリオ: 初期設定で自動キャンセルを有効にすると自動キャンセル時間と前通知設定が表示される
    前提 管理者 "田中" が初期設定の設定画面で自動ステータス更新が有効の状態である
    もし 田中が自動キャンセルを有効にする
    ならば 自動キャンセル時間（秒）と自動キャンセル前通知のON/OFFが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動キャンセル前通知を有効にすると通知時間が表示される
  シナリオ: 初期設定で自動キャンセル前通知を有効にすると前通知時間の入力欄が表示される
    前提 管理者 "田中" が初期設定の設定画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル前通知を有効にする
    ならば 前通知時間（秒）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動延長を有効にすると時間設定が表示される
  シナリオ: 初期設定で自動延長を有効にすると延長タイミングと延長時間の入力欄が表示される
    前提 管理者 "田中" が初期設定の設定画面で自動ステータス更新が有効の状態である
    もし 田中が自動延長を有効にする
    ならば 自動延長タイミング（分）と延長時間（分）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：終了前リマインダーを有効にすると通知設定が表示される
  シナリオ: 初期設定で終了前リマインダーを有効にすると通知時間・繰り返し・音の設定が表示される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が終了前リマインダーを有効にする
    ならば 通知時間（秒）と繰り返し設定とリマインダー音の選択が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：リマインダー繰り返しを回数指定にすると回数入力が表示される
  シナリオ: 初期設定でリマインダー繰り返しを回数指定にすると回数入力欄が表示される
    前提 管理者 "田中" が初期設定の設定画面で終了前リマインダーを有効にしている
    もし 田中がリマインダーの繰り返しを「回数を指定」に切替える
    ならば リマインダー回数（回）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠機能をONにするとデバイスPickerが表示される
  シナリオ: 初期設定で解錠機能を解錠するに切替えると解錠デバイスPickerが表示される
    前提 管理者 "田中" が初期設定の設定画面でデバイス・解施錠セクションを表示している
    もし 田中が解錠機能を「解錠する」に切替える
    ならば 解錠対象デバイス1のPickerが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠機能をOFFにするとデバイスPickerが非表示になる
  シナリオ: 初期設定で解錠機能を解錠しないに切替えると解錠デバイスPickerが非表示になる
    前提 管理者 "田中" が初期設定の設定画面で解錠機能を「解錠する」にしている
    もし 田中が解錠機能を「解錠しない」に切替える
    ならば 解錠対象デバイスPickerが非表示になる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：常時接続を有効にすると対象デバイスが表示される
  シナリオ: 初期設定で常時接続を有効にすると常時接続対象デバイスの選択が表示される
    前提 管理者 "田中" が初期設定の設定画面でデバイス・解施錠セクションを表示している
    もし 田中が常時接続を有効にする
    ならば 常時接続対象デバイスの選択Pickerが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：なりすまし防止を有効にすると詳細設定が表示される
  シナリオ: 初期設定でなりすまし防止を有効にすると防止モード等の設定が表示される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中がなりすまし防止を有効にする
    ならば 防止モード（ハイセキュリティ/スムーズ）と防止期間の設定が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：省エネモードをカスタムにすると条件Pickerが表示される
  シナリオ: 初期設定で省エネモードをカスタムにすると開始・停止条件のPickerが表示される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中が省エネモードを「カスタム」に切替える
    ならば 開始バッテリー残量・開始温度状態・停止バッテリー残量・停止温度状態のPickerが表示される

  # ────────────────────────────────────────
  # ルール⑧: 必須数値フィールドのバリデーション（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：数値フィールドに有効な値を入力
  シナリオ: 初期設定で数値フィールドに有効な値を入力するとバリデーションを通過する
    前提 管理者 "田中" が初期設定の設定画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル時間に有効な値を入力する
    ならば バリデーションエラーは表示されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：1以上が必要なフィールドに0以下を入力
  シナリオ: 初期設定で自動キャンセル時間に0以下を入力して保存を試行するとバリデーションエラーが表示される
    前提 管理者 "田中" が初期設定の設定画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル時間に "0" を入力して保存を試行する
    ならば エラー「自動キャンセル時間は1以上の値を設定してください」が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証必要時間に負の値を入力
  シナリオ: 初期設定で認証必要時間に負の値を入力して保存を試行するとバリデーションエラーが表示される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が認証必要時間に "-1" を入力して保存を試行する
    ならば エラー「認証必要時間は0以上の値を設定してください」が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証成功類似度に範囲外の値を入力
  シナリオ: 初期設定で認証成功類似度に0〜1の範囲外の値を入力して保存を試行するとバリデーションエラーが表示される
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が認証成功類似度に "1.5" を入力して保存を試行する
    ならば エラー「認証成功類似度は0〜1の間の値を設定してください」が表示される
    かつ 設定は保存されない

  # ────────────────────────────────────────
  # ルール⑨: 単独の切替・選択設定の変更と保存（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：テナント従業員による予約の設定切替
  シナリオ: 初期設定でテナント従業員による予約を不許可に切替えると選択状態が変わる
    前提 管理者 "田中" が初期設定の設定画面で認証セクションを表示している
    もし 田中がテナント従業員による予約を「不許可」に切替える
    ならば テナント従業員による予約の選択状態が「不許可」になる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約情報の表示設定切替
  シナリオ: 初期設定で予約情報の表示を非表示に切替えると選択状態が変わる
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中が予約情報の表示を「非表示」に切替える
    ならば 予約情報の選択状態が「非表示」になる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：今すぐ入室の解錠タイミング設定（3択）
  シナリオ: 初期設定で今すぐ入室の解錠タイミングを切替えると選択状態が変わる
    前提 管理者 "田中" が初期設定の設定画面で予約作成（今すぐ入室）が有効かつ解錠機能が有効の状態である
    もし 田中が今すぐ入室の解錠タイミングを「ブッキング防止」に切替える
    ならば 解錠タイミングの選択状態が「ブッキング防止」になる

  # ────────────────────────────────────────
  # ルール⑩: CheckBox群の個別ON/OFFと機能制御（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：タブレットからの機能制御の個別切替
  シナリオ: 初期設定でタブレットからの機能制御を個別にON/OFFするとCheckBox状態が切り替わる
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    もし 田中がタブレットからの機能制御（入室/退室/予約作成/予約延長/予約削除）を個別にON/OFF切替する
    ならば 各機能のCheckBox状態が切り替わる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約作成ONで解錠タイミングが表示される
  シナリオ: 初期設定で機能制御の予約作成をONにすると今すぐ入室の解錠タイミング選択が表示される
    前提 管理者 "田中" が初期設定の設定画面で画面表示・機能制御セクションを表示している
    かつ 予約作成（今すぐ入室）がOFFの状態である
    もし 田中が予約作成（今すぐ入室）をONにする
    ならば 今すぐ入室の解錠タイミング（即時/ブッキング防止/実績管理）の選択が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：顔認証を必要とする機能の個別切替
  シナリオ: 初期設定で顔認証を必要とする機能を個別にON/OFFするとCheckBox状態が切り替わる
    前提 管理者 "田中" が初期設定の設定画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が顔認証を必要とする機能（入室/退室/予約延長）を個別にON/OFF切替する
    ならば 各機能のCheckBox状態が切り替わる

  # ────────────────────────────────────────
  # ルール⑪: Picker選択の設定と保存（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠対象デバイスの選択
  シナリオ: 初期設定で解錠対象デバイスをPickerから選択すると選択値が反映される
    前提 管理者 "田中" が初期設定の設定画面で解錠機能を「解錠する」にしている
    もし 田中が解錠対象デバイス1のPickerからデバイスを選択する
    ならば 選択したデバイスがPickerに反映される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：リマインダー音の選択
  シナリオ: 初期設定でリマインダー音をPickerから選択すると選択値が反映される
    前提 管理者 "田中" が初期設定の設定画面で終了前リマインダーを有効にしている
    もし 田中がリマインダー音のPickerから音を選択する
    ならば 選択した音がPickerに反映される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：終了通知音の選択
  シナリオ: 初期設定で終了通知音をPickerから選択すると選択値が反映される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が終了通知音のPickerから音を選択する
    ならば 選択した音がPickerに反映される

  # ────────────────────────────────────────
  # ルール⑫: テキスト入力の文字数バリデーション（初期設定画面）
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：利用時間外メッセージを入力
  シナリオ: 初期設定で利用時間外メッセージを100文字以内で入力するとバリデーションを通過する
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が利用時間外メッセージ（日本語）に100文字以内のテキストを入力する
    ならば バリデーションエラーは表示されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：利用時間外メッセージが文字数超過
  シナリオ: 初期設定で利用時間外メッセージに101文字以上を入力して保存を試行するとバリデーションエラーが表示される
    前提 管理者 "田中" が初期設定の設定画面でSE・通知セクションを表示している
    もし 田中が利用時間外メッセージ（日本語）に101文字以上のテキストを入力して保存を試行する
    ならば エラー「利用時間外メッセージは100文字以内で入力してください」が表示される
    かつ 設定は保存されない

  # ────────────────────────────────────────
  # E2E: ユーザーストーリー
  # ────────────────────────────────────────

  # @TestLevel: E2E (Cloud実機 / Nightly)
  # 🟢 正常系：ユーザーストーリー
  シナリオ: 【重要動線】初回起動から予約認証モードで初期設定を完了できる
    前提 タブレットは初期設定が未完了の状態である
    かつ 管理者 "田中" がすべての初期設定条件を満たしている
    もし 田中がQR認証を選択し、テンプレートで設定を構成し、予約設定のあるスペースを選択して保存する
    ならば 設定が保存され、初期設定完了が表示される
