# language: ja
機能: 予約認証設定の変更
  管理者が管理者メニューからタブレット設定画面を開き、
  予約認証モードに関連する全設定項目（基本設定・パスワード・認証・画面表示/機能制御・
  利用時間・デバイス/解施錠・SE/通知・顔認証）を変更して保存する。

  背景:
    前提 タブレットは初期設定が完了済みであり、管理者メニューから設定変更画面にアクセスできる状態である

  # ────────────────────────────────────────
  # ルール①: 管理者は予約認証モードの設定を変更して保存できる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：設定変更して保存成功
  シナリオ: 予約認証モードの設定を変更して保存できる
    前提 管理者 "田中" が設定変更画面で予約認証モードの設定を表示している
    もし 田中が認証方法・予約作成可否・自動キャンセル等の設定を変更して保存を実行する
    ならば 変更内容が端末設定として保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：保存後の反映確認
  シナリオ: 設定変更の保存後にホーム画面・カメラ認証画面に変更が反映される
    前提 管理者 "田中" が設定変更を保存して完了遷移を終えている
    ならば 変更がホーム画面およびカメラ認証画面にリアルタイムに反映される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：設定保存APIの失敗
  シナリオ: 設定保存が失敗するとエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面で有効な設定を入力している
    かつ 設定保存APIがエラーを返す状態である
    もし 田中が設定の保存を実行する
    ならば エラーメッセージ "タブレット情報の保存に失敗しました" が表示される
    かつ 設定は保存されない

  # ────────────────────────────────────────
  # ルール②: 予約認証モードのスペース選択には予約設定のあるスペースのみ表示される
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約設定ありのスペースが選択項目に表示される
  シナリオ: 予約設定のあるスペースのみがスペース選択項目に表示される
    前提 管理者 "田中" が設定変更画面でスペース選択項目を表示している
    かつ スペース "会議室A" には予約設定（通常予約またはサードプレイス）が登録されている
    ならば "会議室A" がスペース選択項目に表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約設定なしのスペースは選択項目に表示されない
  シナリオ: 予約設定のないスペースはスペース選択項目に表示されない
    前提 管理者 "田中" が設定変更画面でスペース選択項目を表示している
    かつ スペース "会議室B" には予約設定が登録されていない
    ならば "会議室B" はスペース選択項目に表示されない

  # ────────────────────────────────────────
  # ルール③: 顔認証を含まない認証方法では顔認証必須の機能を有効化できない
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：顔認証を含む認証方法で予約作成を有効化
  シナリオ: 顔認証を含む認証方法で予約作成機能を有効化できる
    前提 管理者 "田中" が設定変更画面で顔認証を含む認証方法（顔認証またはQR＋顔認証）を選択している
    もし 田中が予約作成（今すぐ入室）を有効化する
    ならば 予約作成機能が有効化される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：QR認証のみで予約作成を有効化しようとする
  シナリオ: QR認証のみでは予約作成機能を有効化できず認証方法の変更導線が表示される
    前提 管理者 "田中" が設定変更画面でQR認証のみを選択している
    もし 田中が予約作成（今すぐ入室）を有効化しようとする
    ならば エラーメッセージ "『タブレットからの機能制御』>『予約作成（今すぐ入室）』が不適切です" が表示される
    かつ エラーダイアログ内に認証方法の変更導線が提示される
    かつ 予約作成機能は有効化されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：QR認証のみで予約削除を有効化しようとする
  シナリオ: QR認証のみでは予約削除機能を有効化できず認証方法の変更導線が表示される
    前提 管理者 "田中" が設定変更画面でQR認証のみを選択している
    もし 田中が予約削除を有効化しようとする
    ならば エラーメッセージ "『タブレットからの機能制御』>『予約削除』が不適切です" が表示される
    かつ エラーダイアログ内に認証方法の変更導線が提示される
    かつ 予約削除機能は有効化されない

  # ────────────────────────────────────────
  # ルール④: バリデーションエラーがある場合はセクション別にエラーが表示され保存できない
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：必須項目未入力で保存拒否
  シナリオ: 必須項目が未入力の場合はエラーダイアログにエラー詳細が表示される
    前提 管理者 "田中" が設定変更画面で必須項目（タブレット名/スペース/予約認証方法等）を未入力にしている
    もし 田中が設定の保存を実行する
    ならば エラーダイアログに該当セクションのエラー詳細が表示される（最大3件、超過分は「他N件」）
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：数値項目に不正値を入力
  シナリオ: 数値入力項目に0以下の値を入力すると保存が拒否される
    前提 管理者 "田中" が設定変更画面で自動キャンセル時間に "0" を入力している
    もし 田中が設定の保存を実行する
    ならば エラーダイアログに "1以上の値を設定してください" が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：パスワード不一致で保存拒否
  シナリオ: パスワードと確認パスワードが一致しない場合は保存が拒否される
    前提 管理者 "田中" が設定変更画面でパスワードに "pass123" を入力している
    かつ 確認パスワードに "pass456" を入力している
    もし 田中が設定の保存を実行する
    ならば エラーダイアログに "パスワードが一致しません" が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：複数セクションのエラーバッジ表示
  シナリオ: 複数セクションにバリデーションエラーがある場合は各セクションにエラー件数バッジが表示される
    前提 管理者 "田中" が設定変更画面で複数セクションの必須項目を未入力にしている
    もし 田中が設定の保存を実行する
    ならば 各セクションのサイドバー項目にエラー件数バッジが表示される
    かつ 設定は保存されない

  # ────────────────────────────────────────
  # ルール⑤: 設定変更をキャンセルすると変更前の状態に戻る
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：設定変更のキャンセル
  シナリオ: 設定変更をキャンセルすると変更が破棄され管理者メニューに戻る
    前提 管理者 "田中" が管理者メニュー経由で設定変更画面を開き、設定を変更している
    もし 田中が設定変更をキャンセルする
    ならば 変更が破棄され管理者メニューに戻る

  # ────────────────────────────────────────
  # ルール⑥: 設定保存成功後、設定コピーの登録と完了遷移が行われる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：コピーして管理者メニューへ戻る
  シナリオ: 保存後に設定コピーを登録して管理者メニューに戻る
    前提 管理者 "田中" が設定を保存し、設定コピー確認が表示されている
    もし 田中が「コピーする」を選択し、識別名称 "本社1F会議室設定" を入力して登録する
    ならば 設定コピーが登録され、管理者メニューに戻る

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：コピーしないで管理者メニューへ戻る
  シナリオ: 保存後にコピーしないを選択して管理者メニューに戻る
    前提 管理者 "田中" が設定を保存し、設定コピー確認が表示されている
    もし 田中が「コピーしない」を選択する
    ならば 管理者メニューに戻る

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：識別名称未入力でコピー登録不可
  シナリオ: 識別名称を入力しなければ設定コピーの登録ができない
    前提 管理者 "田中" が設定の保存に成功し、「コピーする」を選択している
    かつ 識別名称が未入力である
    ならば 登録ボタンは無効化されている

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟡 例外系：設定コピー登録APIの失敗
  シナリオ: 設定コピーの登録が失敗するとエラーメッセージが表示される
    前提 管理者 "田中" が設定の保存に成功し、設定コピー確認が表示されている
    かつ 設定コピー登録APIがエラーを返す状態である
    もし 田中が「コピーする」を選択し、識別名称を入力して登録する
    ならば エラーメッセージ "タブレット情報の保存に失敗しました" が表示される

  # ────────────────────────────────────────
  # ルール⑦: 保存時に常時接続推奨ダイアログが表示される場合がある
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：常時接続推奨ダイアログで「有効化する」を選択
  シナリオ: 常時接続推奨ダイアログで有効化を選択すると常時接続が有効になり保存が続行される
    前提 管理者 "田中" が設定変更画面で解錠デバイス1台＋Gate型＋常時接続無効の状態で保存を実行している
    かつ 常時接続推奨ダイアログが表示されている
    もし 田中が「有効化する」を選択する
    ならば 常時接続が有効になり保存が続行される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：常時接続推奨ダイアログで「そのまま保存する」を選択
  シナリオ: 常時接続推奨ダイアログでそのまま保存を選択すると常時接続無効のまま保存される
    前提 管理者 "田中" が設定変更画面で解錠デバイス1台＋Gate型＋常時接続無効の状態で保存を実行している
    かつ 常時接続推奨ダイアログが表示されている
    もし 田中が「そのまま保存する」を選択する
    ならば 常時接続は無効のまま保存が続行される

  # ────────────────────────────────────────
  # ルール⑧: 設定編集中に既存設定をコピーして上書きできる
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：テンプレートを選択してコピー
  シナリオ: 既存設定コピーでテンプレートを選択すると入力内容が破棄されテンプレートの設定値が適用される
    前提 管理者 "田中" が設定変更画面で設定を編集中である
    もし 田中が既存設定のコピーを実行する
    かつ 確認ダイアログで「既存設定をコピー」を選択する
    かつ テンプレートを選択する
    ならば 入力済みの内容が破棄され、テンプレートの設定値が適用される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：コピーした端末設定を選択してコピー
  シナリオ: 既存設定コピーでコピー元の端末設定を選択すると入力内容が破棄されその設定値が適用される
    前提 管理者 "田中" が設定変更画面で設定を編集中である
    かつ 他のタブレットで保存された既存設定が1件以上存在する
    もし 田中が既存設定のコピーを実行する
    かつ 確認ダイアログで「既存設定をコピー」を選択する
    かつ コピー元の端末設定を選択する
    ならば 入力済みの内容が破棄され、コピー元の設定値が適用される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：既存設定コピーをキャンセル
  シナリオ: 既存設定コピーの確認ダイアログでキャンセルすると現在の入力が維持される
    前提 管理者 "田中" が設定変更画面で設定を編集中である
    もし 田中が既存設定のコピーを実行する
    かつ 確認ダイアログで「キャンセル」を選択する
    ならば ダイアログが閉じ、現在の入力内容が維持される

  # ────────────────────────────────────────
  # ルール⑨: ON/OFF切替で子項目の表示が連動する設定
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動ステータス更新を有効にすると子項目群が表示される
  シナリオ: 自動ステータス更新を有効にすると自動キャンセル・自動延長の設定が表示される
    前提 管理者 "田中" が設定変更画面で画面表示・機能制御セクションを表示している
    もし 田中が自動ステータス更新を有効にする
    ならば 自動キャンセルと自動延長の設定項目群が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動ステータス更新を無効にすると子項目群が非表示になる
  シナリオ: 自動ステータス更新を無効にすると自動キャンセル・自動延長と全子項目が非表示になる
    前提 管理者 "田中" が設定変更画面で自動ステータス更新を有効にしている
    かつ 自動キャンセルと自動延長の設定項目が表示されている
    もし 田中が自動ステータス更新を無効にする
    ならば 自動キャンセル・自動延長と全子項目が非表示になる
    かつ 非表示になった項目はデフォルト値に復元される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動キャンセルを有効にすると時間と前通知が表示される
  シナリオ: 自動キャンセルを有効にすると自動キャンセル時間と前通知設定が表示される
    前提 管理者 "田中" が設定変更画面で自動ステータス更新が有効の状態である
    もし 田中が自動キャンセルを有効にする
    ならば 自動キャンセル時間（秒）と自動キャンセル前通知のON/OFFが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動キャンセル前通知を有効にすると通知時間が表示される
  シナリオ: 自動キャンセル前通知を有効にすると前通知時間の入力欄が表示される
    前提 管理者 "田中" が設定変更画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル前通知を有効にする
    ならば 前通知時間（秒）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：自動延長を有効にすると時間設定が表示される
  シナリオ: 自動延長を有効にすると延長タイミングと延長時間の入力欄が表示される
    前提 管理者 "田中" が設定変更画面で自動ステータス更新が有効の状態である
    もし 田中が自動延長を有効にする
    ならば 自動延長タイミング（分）と延長時間（分）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：終了前リマインダーを有効にすると通知設定が表示される
  シナリオ: 終了前リマインダーを有効にすると通知時間・繰り返し・音の設定が表示される
    前提 管理者 "田中" が設定変更画面でSE・通知セクションを表示している
    もし 田中が終了前リマインダーを有効にする
    ならば 通知時間（秒）と繰り返し設定とリマインダー音の選択が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：リマインダー繰り返しを回数指定にすると回数入力が表示される
  シナリオ: リマインダー繰り返しを回数指定にすると回数入力欄が表示される
    前提 管理者 "田中" が設定変更画面で終了前リマインダーを有効にしている
    もし 田中がリマインダーの繰り返しを「回数を指定」に切替える
    ならば リマインダー回数（回）の入力欄が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠機能をONにするとデバイスPickerが表示される
  シナリオ: 解錠機能を解錠するに切替えると解錠デバイスPickerが表示される
    前提 管理者 "田中" が設定変更画面でデバイス・解施錠セクションを表示している
    もし 田中が解錠機能を「解錠する」に切替える
    ならば 解錠対象デバイス1のPickerが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠機能をOFFにするとデバイスPickerが非表示になる
  シナリオ: 解錠機能を解錠しないに切替えると解錠デバイスPickerが非表示になる
    前提 管理者 "田中" が設定変更画面で解錠機能を「解錠する」にしている
    もし 田中が解錠機能を「解錠しない」に切替える
    ならば 解錠対象デバイスPickerが非表示になる

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：常時接続を有効にすると対象デバイスが表示される
  シナリオ: 常時接続を有効にすると常時接続対象デバイスの選択が表示される
    前提 管理者 "田中" が設定変更画面でデバイス・解施錠セクションを表示している
    もし 田中が常時接続を有効にする
    ならば 常時接続対象デバイスの選択Pickerが表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：なりすまし防止を有効にすると詳細設定が表示される
  シナリオ: なりすまし防止を有効にすると防止モード等の設定が表示される
    前提 管理者 "田中" が設定変更画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中がなりすまし防止を有効にする
    ならば 防止モード（ハイセキュリティ/スムーズ）と防止期間の設定が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：省エネモードをカスタムにすると条件Pickerが表示される
  シナリオ: 省エネモードをカスタムにすると開始・停止条件のPickerが表示される
    前提 管理者 "田中" が設定変更画面で画面表示・機能制御セクションを表示している
    もし 田中が省エネモードを「カスタム」に切替える
    ならば 開始バッテリー残量・開始温度状態・停止バッテリー残量・停止温度状態のPickerが表示される

  # ────────────────────────────────────────
  # ルール⑩: 必須数値フィールドのバリデーション
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：数値フィールドの値を変更して保存
  シナリオ: 数値フィールドの値を変更して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル時間を有効な値に変更して保存を実行する
    ならば 変更した自動キャンセル時間が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：1以上が必要なフィールドに0以下を入力
  シナリオ: 自動キャンセル時間に0以下を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面で自動キャンセルが有効の状態である
    もし 田中が自動キャンセル時間に "0" を入力して保存を実行する
    ならば エラー「自動キャンセル時間は1以上の値を設定してください」が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：自動延長タイミングに0以下を入力
  シナリオ: 自動延長タイミングに0以下を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面で自動延長が有効の状態である
    もし 田中が自動延長タイミングに "0" を入力して保存を実行する
    ならば エラー「自動延長タイミングは1以上の値を設定してください」が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：リマインダー回数に0以下を入力
  シナリオ: リマインダー回数に0以下を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面で終了前リマインダーが有効かつ回数指定の状態である
    もし 田中がリマインダー回数に "0" を入力して保存を実行する
    ならば エラー「終了前リマインダーの回数は1以上の値を指定してください」が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証必要時間に負の値を入力
  シナリオ: 認証必要時間に負の値を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が認証必要時間に "-1" を入力して保存を実行する
    ならば エラー「認証必要時間は0以上の値を設定してください」が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証成功類似度に範囲外の値を入力
  シナリオ: 認証成功類似度に0〜1の範囲外の値を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が認証成功類似度に "1.5" を入力して保存を実行する
    ならば エラー「認証成功類似度は0〜1の間の値を設定してください」が表示される

  # ────────────────────────────────────────
  # ルール⑪: 単独の切替・選択設定の変更と保存
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：テナント従業員による予約の設定変更
  シナリオ: テナント従業員による予約を不許可に変更して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で認証セクションを表示している
    もし 田中がテナント従業員による予約を「不許可」に変更して保存を実行する
    ならば テナント従業員による予約の不許可設定が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約情報の表示設定変更
  シナリオ: 予約情報の表示を非表示に変更して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で画面表示・機能制御セクションを表示している
    もし 田中が予約情報の表示を「非表示」に変更して保存を実行する
    ならば 予約情報の非表示設定が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：空室時の自動施錠設定変更
  シナリオ: 空室時の自動施錠を無効に変更して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面でデバイス・解施錠セクションを表示している
    もし 田中が空室時の自動施錠を「無効」に変更して保存を実行する
    ならば 空室時の自動施錠の無効設定が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：今すぐ入室の解錠タイミング変更（3択）
  シナリオ: 今すぐ入室の解錠タイミングを変更して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で予約作成（今すぐ入室）が有効かつ解錠機能が有効の状態である
    もし 田中が今すぐ入室の解錠タイミングを「ブッキング防止」に変更して保存を実行する
    ならば 解錠タイミングのブッキング防止設定が保存される

  # ────────────────────────────────────────
  # ルール⑫: CheckBox群の個別ON/OFFと機能制御
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：認証方法の変更
  シナリオ: 予約認証の認証方法を変更して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で認証セクションを表示している
    もし 田中が認証方法のチェックボックス（QR認証/顔認証）を変更して保存を実行する
    ならば 選択した認証方法が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：認証方法を全て未選択にして保存
  シナリオ: 予約認証の認証方法を全て未選択にすると保存が拒否される
    前提 管理者 "田中" が設定変更画面で認証セクションを表示している
    もし 田中が認証方法のチェックボックスを全て未選択にして保存を実行する
    ならば エラー「予約認証の認証方法を選択してください」が表示される
    かつ 設定は保存されない

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：タブレットからの機能制御の個別切替
  シナリオ: タブレットからの機能制御を個別にON/OFFして保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で画面表示・機能制御セクションを表示している
    もし 田中がタブレットからの機能制御（入室/退室/予約作成/予約延長/予約削除）を個別にON/OFF切替して保存を実行する
    ならば 各機能の有効/無効が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：予約作成ONで解錠タイミングが表示される
  シナリオ: 機能制御の予約作成をONにすると今すぐ入室の解錠タイミング選択が表示される
    前提 管理者 "田中" が設定変更画面で画面表示・機能制御セクションを表示している
    かつ 予約作成（今すぐ入室）がOFFの状態である
    もし 田中が予約作成（今すぐ入室）をONにする
    ならば 今すぐ入室の解錠タイミング（即時/ブッキング防止/実績管理）の選択が表示される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：顔認証を必要とする機能の個別切替
  シナリオ: 顔認証を必要とする機能を個別にON/OFFして保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で顔認証セクションを表示している
    かつ 顔認証オプションが有効である
    もし 田中が顔認証を必要とする機能（入室/退室/予約延長）を個別にON/OFF切替して保存を実行する
    ならば 各機能に顔認証が必要かどうかの設定が保存される

  # ────────────────────────────────────────
  # ルール⑬: Picker選択の変更と保存
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：解錠対象デバイスの選択
  シナリオ: 解錠対象デバイスを選択して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で解錠機能を「解錠する」にしている
    もし 田中が解錠対象デバイス1のPickerからデバイスを選択して保存を実行する
    ならば 選択したデバイスが保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：常時接続対象デバイスの選択
  シナリオ: 常時接続対象デバイスを選択して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で常時接続を有効にしている
    もし 田中が常時接続対象デバイスのPickerからデバイスを選択して保存を実行する
    ならば 選択したデバイスが保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：リマインダー音の選択
  シナリオ: リマインダー音を選択して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面で終了前リマインダーを有効にしている
    もし 田中がリマインダー音のPickerから音を選択して保存を実行する
    ならば 選択した音が保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：終了通知音の選択
  シナリオ: 終了通知音を選択して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面でSE・通知セクションを表示している
    もし 田中が終了通知音のPickerから音を選択して保存を実行する
    ならば 選択した音が保存される

  # ────────────────────────────────────────
  # ルール⑭: テキスト入力の文字数バリデーション
  # ────────────────────────────────────────

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🟢 正常系：利用時間外メッセージを入力して保存
  シナリオ: 利用時間外メッセージを100文字以内で入力して保存すると変更が反映される
    前提 管理者 "田中" が設定変更画面でSE・通知セクションを表示している
    もし 田中が利用時間外メッセージ（日本語）に100文字以内のテキストを入力して保存を実行する
    ならば メッセージが保存される

  # @TestLevel: PresentationUnit (JVM / Turbine)
  # 🔴 異常系：利用時間外メッセージが文字数超過
  シナリオ: 利用時間外メッセージに101文字以上を入力すると専用のエラーメッセージが表示される
    前提 管理者 "田中" が設定変更画面でSE・通知セクションを表示している
    もし 田中が利用時間外メッセージ（日本語）に101文字以上のテキストを入力して保存を実行する
    ならば エラー「利用時間外メッセージは100文字以内で入力してください」が表示される

  # ────────────────────────────────────────
  # E2E: ユーザーストーリー
  # ────────────────────────────────────────

  # @TestLevel: E2E (Cloud実機 / Nightly)
  # 🟢 正常系：主要動線（設定変更→保存→完了）
  シナリオ: 【重要動線】管理者メニューから予約認証設定を変更して保存し完了できる
    前提 タブレットは初期設定が完了済みである
    かつ 管理者 "田中" が管理者メニューから設定変更画面を開いている
    もし 田中が予約認証モードの設定を変更し、予約設定のあるスペースを選択して保存する
    ならば 設定が保存され、設定コピー確認を経て管理者メニューに戻る
