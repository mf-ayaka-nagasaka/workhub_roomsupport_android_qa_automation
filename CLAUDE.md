# プロダクトの概要
- **プロダクト名**: workhub Room Support
- **プラットフォーム**: 会議室に設置されるタブレット用アプリケーション
- **目的**: 会議室の利用状況の可視化と、予約・利用手続きの効率化

# コアドメイン・主要機能
当アプリは、以下の主要な機能と状態遷移を管理します。

- **予約状態の可視化**: 現在の空室/利用中ステータスの表示。
- **予約操作**: タブレット上からの即時予約、および利用時間の延長操作。
- **空予約（ゴースト予約）防止**: 予約開始時刻を過ぎても「利用開始」アクションがない場合の自動キャンセル処理。
- **終了前通知**: 予約終了時刻が近づいた際のアラート表示機能。
- **ハードウェア連携（オプション）**: 
  - 顔認証デバイスとの連携（顔認証による予約実行）。
  - スマートロックとの連携（顔認証による会議室の解錠処理）。

# QAの対象範囲（スコープ）
本リポジトリにおけるQAおよびテスト設計のスコープは以下の通りです。現状は手動テストのケース作成・管理がメインですが、将来的なテスト自動化（Android向け）を視野に入れた構造化を行います。

## 対象プラットフォーム
- **Android アプリケーション**

## テスト対象（In-Scope）
- **対象機能の確認テスト**: 新規開発や改修が行われた機能の正当性検証。
- **リグレッションテスト**: 変更による影響が及ぶ範囲の退行テスト。

## 対象外（Out-of-Scope）
AIによるテストケース生成やコード作成において、以下はスコープ外として扱ってください。

- **影響範囲外の既存機能**: プログラムの変更がなく、影響が及ばない既存機能のテストは不要。
- **非機能要件の検証**: 性能（パフォーマンス）、負荷、セキュリティに関するテストは別計画となるため本スコープからは除外。
- **ホワイトボックス検証**: 原則としてブラックボックス検証とする。機能の合否判定は、アプリの画面表示やAPIレスポンス等「ユーザーから観測できる結果」で行う（ただし、エラー原因の切り分け等のデバッグ目的でのFirebase等の活用は許容する）。
- **外部SDKの内部ロジック検証**: 顔認証SDK（Android版）、BitLock SDK、Firebase、Datadog等のサードパーティ製SDK自体の動作保証はベンダー責務とする。テストケースは「アプリがSDKを正しく呼び出し、その結果（成功/失敗）を正しくハンドリングできているか」に留めること。
- **バックエンドの内部ロジック検証**: サーバー側の処理の正当性は対象外。アプリ側のテストとしては「APIからの特定レスポンス（正常系・異常系）を受け取った際の状態遷移やUI表示が正しいか」を検証すること。

# テスト管理ツールと役割
- **Notion**: テスト設計（テスト観点）、実装タスク、不具合チケットの管理
- **Qase**: テストケースの管理・実行（※インポート用CSVファイルで運用）
- **テストケース作成（テスト実装）**: 専用のSkillを参照し、指定のCSVフォーマットで出力すること。

# テスト環境・クレデンシャルに関するルール
セキュリティ保護のため、具体的な環境情報に対する以下のルールを厳守してください。

- **情報の抽象化（プレースホルダーの利用）**: 
  テストケースを作成する際、テスト環境のURL、テスト用アカウント（ID/パスワード）、APIキーなどは絶対に直接記述（ハードコード）しないでください。
  代わりに「QA環境」「Staging環境」「[テスト用管理者アカウント]」「[顔認証用ダミーデータ]」などの抽象的な名称（プレースホルダー）を使用してください。
  ただし、Notion等の社内ドキュメントへの参照リンク（手順書・仕様書等）はテスト環境情報に該当しないため、前提条件や手順に記載して問題ありません。
- **クレデンシャル情報の管理**: 
  実際のログイン情報や環境URLは、Notionのセキュリティ制限されたページで人間が管理・参照します。AIはそれらを推測・生成する必要はありません。

# テストデータの扱い方に関するルール
AIが手動テストのケース（手順）を生成する際は、テストデータの取り扱いについて以下の点を含めるようにしてください。

- **事前準備（Setup）の明記**: 
  テストを実行するために必要なデータ状態（例：「〇〇会議室に30分後の予約が1件入っている状態」「顔情報が未登録のユーザー」など）を、前提条件として明確に記載してください。
- **事後処理（Teardown）の明記**: 
  テスト実行によって生成されたデータ（例：「テスト中に作成した予約」「登録した顔写真データ」など）を、テスト終了後に削除・リセットする必要がある場合は、手順の最後にその旨を記載してください。
- **データ依存の排除（状態の独立性）**: 
  各テストケースは、原則として他のテストケースの実行結果（残存データ）に依存せず、単独で検証を完結できる前提で手順を設計してください。
  
# ドキュメント（README等）の保守ルール
本プロジェクトのドキュメントを常に最新に保つため、以下のルールに従って保守をサポートしてください。

## 更新時の大原則
- **承認の必須化**: ドキュメントを更新する際は、必ず事前に変更内容（差分）をユーザーに提案し、**承認を得てから実行**してください。自動で上書きしてはいけません。
- **構成の維持**: 既存のドキュメントの全体構成（見出し、レイアウト、トーン＆マナー）は絶対に破壊せず、変更が必要な該当箇所のみを部分的に更新してください。

## 更新のトリガーと対象ファイル
以下の作業や変更が発生した場合は、対象ファイルの該当箇所に更新が必要かを確認し、提案してください。

1. **リポジトリ構成の変更（必須対応）**
   - **トリガー**: フォルダ構成や重要なファイルが追加・削除・移動された場合。
   - **確認対象**: `README.md` の「リポジトリ構成」に関するセクション。
2. **Skillの追加・更新**
   - **トリガー**: Claude CodeのSkillを新規追加、または既存Skillの処理を変更した場合。
   - **確認対象**: `README.md` の「Skill」に関するセクション、および `.claude/README.md` 。
3. **仕様や機能一覧の更新**
   - **トリガー**: アプリの機能一覧や仕様書のテキストが追加・変更された場合。
   - **確認対象**: `README.md` の「機能」に関するセクション。
